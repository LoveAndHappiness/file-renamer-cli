{
  "name": "getNewFileName",
  "description": "Generates a new file name based on the content of the file",
  "parameters": {
    "type": "object",
    "properties": {
      "fileContent": {
        "type": "string",
        "description": "A text snippet or summary from the file"
      }
    },
    "required": [
      "fileContent"
    ]
  }
}